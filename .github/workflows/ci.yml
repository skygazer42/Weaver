name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Dependency sanity (pip check)
        run: python -m pip check

      - name: Compile (compileall)
        run: python -m compileall -q .

      - name: Export OpenAPI spec
        run: python scripts/export_openapi.py --output /tmp/weaver-openapi.json

      - name: Upload OpenAPI spec artifact
        uses: actions/upload-artifact@v7
        with:
          name: weaver-openapi-spec
          path: /tmp/weaver-openapi.json

      - name: Secret scan
        run: python scripts/secret_scan.py

      - name: Lint (ruff, changed files)
        run: bash scripts/ruff_changed_files.sh

      - name: Tests (pytest)
        run: pytest -q

  frontend:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm -C web install --frozen-lockfile

      - name: Download OpenAPI spec artifact
        uses: actions/download-artifact@v6
        with:
          name: weaver-openapi-spec
          path: .tmp/openapi

      - name: OpenAPI → TS types drift guard
        run: bash scripts/check_openapi_ts_types.sh --openapi-json .tmp/openapi/weaver-openapi.json

      - name: Lint (next lint)
        run: pnpm -C web lint

      - name: Build (next build)
        run: pnpm -C web build

  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build image
        run: docker build -f docker/Dockerfile .

  benchmark_smoke:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Run benchmark smoke
        run: |
          python scripts/benchmark_deep_research.py \
            --max-cases 3 \
            --mode auto \
            --output /tmp/benchmark_smoke.json

      - name: Upload benchmark artifact
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-smoke-report
          path: /tmp/benchmark_smoke.json
