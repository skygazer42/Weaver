# Weaver Agent 可视化过程实施计划

## 🎯 目标

实现类似 ChatGPT/Manus 的 Agent 功能：
- ✅ 可以看到网页搜索/浏览的过程
- ✅ 实时展示 Agent 操作步骤
- ✅ 每步操作返回截图
- ✅ 任务进度可视化

## 📊 当前状态 vs 目标状态

| 功能 | 当前 Weaver | Manus | 目标 |
|------|------------|-------|------|
| 浏览器工具 | ✅ 有 | ✅ 有 | ✅ |
| 截图功能 | ✅ 有 | ✅ 有 | ✅ |
| **实时流式展示过程** | ❌ 缺失 | ✅ 有 | ✅ |
| **任务步骤可视化** | ❌ 缺失 | ✅ 有 | ✅ |
| **截图实时推送** | ❌ 缺失 | ✅ 有 | ✅ |
| 前端进度条 | ❌ 缺失 | ✅ 有 | ✅ |

## 🚀 20 步实施计划

---

### 阶段一：基础架构（步骤 1-5）

#### **步骤 1：分析 Manus 的流式响应机制**
- **目标**：理解 Manus 如何实现工具执行过程的实时推送
- **文件参考**：
  - `agentpress/response_processor.py` - 响应处理
  - `agentpress/thread_manager.py` - 线程管理
- **交付物**：流式响应机制设计文档
- **时间**：分析代码，提取关键设计

#### **步骤 2：设计 Weaver 的工具执行事件系统**
- **目标**：定义工具执行过程中的事件类型
- **事件类型**：
  ```python
  class ToolEvent:
      TOOL_START = "tool_start"      # 工具开始执行
      TOOL_PROGRESS = "tool_progress" # 执行进度
      TOOL_SCREENSHOT = "tool_screenshot"  # 截图事件
      TOOL_RESULT = "tool_result"    # 执行结果
      TOOL_ERROR = "tool_error"      # 执行错误
  ```
- **交付物**：`agent/events.py`

#### **步骤 3：创建截图存储和服务机制**
- **目标**：实现截图保存和 HTTP 访问
- **参考 Manus**：`sb_browser_tool.py` 的 `_take_screenshot_and_save()` 方法
- **实现内容**：
  ```python
  # 1. 截图保存到 screenshots/ 目录
  # 2. 创建 /api/screenshots/{filename} 端点
  # 3. 截图 URL 生成和返回
  ```
- **交付物**：
  - `tools/screenshot_service.py`
  - `main.py` 添加截图 API 路由

#### **步骤 4：重构 sandbox_browser_tools 支持事件发送**
- **目标**：每个操作发送事件，包括截图
- **修改文件**：`tools/sandbox_browser_tools.py`
- **改造示例**：
  ```python
  async def _run(self, url: str, ...):
      # 发送 TOOL_START 事件
      await self._emit_event(ToolEvent.TOOL_START, {"action": "navigate", "url": url})

      # 执行操作
      page.goto(url, ...)

      # 截图
      screenshot = await self._take_screenshot("navigate")
      await self._emit_event(ToolEvent.TOOL_SCREENSHOT, screenshot)

      # 发送结果
      await self._emit_event(ToolEvent.TOOL_RESULT, {...})
  ```
- **交付物**：改造后的 `sandbox_browser_tools.py`

#### **步骤 5：实现事件到 SSE 的转换**
- **目标**：将工具事件转换为 SSE 流式输出
- **参考 Manus**：`agentpress/response_processor.py` 的流式处理
- **实现位置**：`main.py` 的 `/api/chat` 端点
- **交付物**：SSE 事件格式定义和发送逻辑

---

### 阶段二：核心功能（步骤 6-12）

#### **步骤 6：参考 Manus 创建 TaskListTool**
- **目标**：实现任务列表工具，展示执行步骤
- **参考文件**：`agent/tools/task_list_tool.py`（40KB）
- **功能**：
  - 创建任务
  - 更新任务状态（pending/running/completed/failed）
  - 任务进度百分比
- **交付物**：`tools/task_list_tool.py`

#### **步骤 7：参考 Manus 重构 SandboxBrowserTool**
- **目标**：完整实现沙盒浏览器工具
- **参考文件**：`agent/tools/sb_browser_tool.py`（42KB）
- **关键功能**：
  - `browser_navigate_to` - 导航
  - `browser_click_element` - 点击
  - `browser_input_text` - 输入
  - `browser_scroll_down` - 滚动
  - `browser_send_keys` - 按键
- **每个操作都返回截图**
- **交付物**：改进的 `tools/sandbox_browser_tools.py`

#### **步骤 8：参考 Manus 创建 SandboxWebSearchTool**
- **目标**：实现沙盒内网页搜索
- **参考文件**：`agent/tools/sandbox_web_search_tool.py`（14KB）
- **功能**：
  - 使用搜索引擎搜索
  - 解析搜索结果
  - 返回结构化结果 + 截图
- **交付物**：`tools/sandbox_web_search_tool.py`

#### **步骤 9：创建 ComputerUseTool（可选）**
- **目标**：实现电脑操作工具
- **参考文件**：`agent/tools/computer_use_tool.py`（22KB）
- **功能**：
  - 屏幕截图
  - 鼠标点击/移动
  - 键盘输入
- **交付物**：`tools/computer_use_tool.py`

#### **步骤 10：实现工具执行的并行/串行策略**
- **目标**：支持工具的并行和串行执行
- **参考 Manus**：`ProcessorConfig.tool_execution_strategy`
- **配置**：
  ```python
  class ToolExecutionConfig:
      strategy: Literal["sequential", "parallel"] = "sequential"
      max_parallel: int = 3
  ```
- **交付物**：`agent/tool_executor.py`

#### **步骤 11：实现上下文管理器**
- **目标**：管理对话历史，避免超出 token 限制
- **参考 Manus**：`agentpress/context_manager.py`
- **功能**：
  - 消息截断
  - 重要信息保留
  - token 计数
- **交付物**：`agent/context_manager.py`

#### **步骤 12：实现 Agent 循环控制**
- **目标**：控制 Agent 的迭代次数和自动继续
- **参考 Manus**：`agent/run.py` 的 `AgentConfig`
- **配置**：
  ```python
  class AgentRunConfig:
      max_iterations: int = 100
      native_max_auto_continues: int = 3
      enable_thinking: bool = False
  ```
- **交付物**：改进的 `agent/nodes.py`

---

### 阶段三：集成和优化（步骤 13-17）

#### **步骤 13：创建统一的工具注册中心**
- **目标**：参考 Manus 的 ToolManager 模式
- **参考文件**：`agent/run.py` 的 `ToolManager` 类
- **功能**：
  - 统一注册所有工具
  - 动态启用/禁用工具
  - 工具依赖管理
- **交付物**：改进的 `agent/agent_tools.py`

#### **步骤 14：实现 Agent 状态持久化**
- **目标**：保存 Agent 执行状态，支持恢复
- **参考 Manus**：`agentpress/thread_manager.py` 的数据库操作
- **功能**：
  - 消息持久化
  - 工具调用记录
  - 截图关联
- **交付物**：数据库 schema 更新 + 服务代码

#### **步骤 15：优化 SSE 流式响应格式**
- **目标**：定义清晰的 SSE 事件格式
- **事件格式**：
  ```json
  // 工具开始
  {"type": "tool_start", "data": {"tool": "browser_navigate", "args": {...}}}

  // 截图
  {"type": "screenshot", "data": {"url": "/api/screenshots/xxx.png"}}

  // 任务进度
  {"type": "task_update", "data": {"id": "1", "status": "running", "progress": 50}}

  // 文本内容
  {"type": "content", "data": {"text": "..."}}

  // 完成
  {"type": "done", "data": {"message": "任务完成"}}
  ```
- **交付物**：SSE 事件规范文档 + 代码实现

#### **步骤 16：添加错误处理和重试机制**
- **目标**：优雅处理工具执行错误
- **参考 Manus**：错误处理模式
- **功能**：
  - 工具执行超时
  - 自动重试
  - 错误恢复
- **交付物**：错误处理中间件

#### **步骤 17：实现浏览器会话复用**
- **目标**：同一对话复用浏览器实例
- **参考**：当前的 `sandbox_browser_session.py`
- **优化**：
  - 浏览器预热
  - 会话超时清理
  - 资源限制
- **交付物**：改进的 `tools/sandbox_browser_session.py`

---

### 阶段四：前端适配和文档（步骤 18-20）

#### **步骤 18：创建前端 SSE 事件处理示例**
- **目标**：提供前端如何消费 SSE 事件的示例
- **内容**：
  ```javascript
  const eventSource = new EventSource('/api/chat?stream=true');

  eventSource.onmessage = (event) => {
      const data = JSON.parse(event.data);

      switch(data.type) {
          case 'tool_start':
              showToolStarting(data.data);
              break;
          case 'screenshot':
              displayScreenshot(data.data.url);
              break;
          case 'task_update':
              updateTaskProgress(data.data);
              break;
          case 'content':
              appendContent(data.data.text);
              break;
      }
  };
  ```
- **交付物**：`docs/FRONTEND_INTEGRATION.md`

#### **步骤 19：编写完整的 API 文档**
- **目标**：文档化所有新增 API 和事件
- **内容**：
  - SSE 事件格式
  - 截图 API
  - 工具调用格式
  - 配置选项
- **交付物**：`docs/AGENT_VISUAL_API.md`

#### **步骤 20：创建端到端测试和演示**
- **目标**：验证完整功能
- **测试用例**：
  1. 简单网页浏览 + 截图
  2. 搜索 + 多步操作
  3. 表单填写 + 提交
  4. 长任务 + 进度展示
- **交付物**：
  - `tests/test_agent_visual.py`
  - 演示脚本和录屏

---

## 📁 文件变更清单

### 新增文件
```
agent/
  ├── events.py              # 事件系统
  ├── tool_executor.py       # 工具执行器
  └── context_manager.py     # 上下文管理

tools/
  ├── screenshot_service.py  # 截图服务
  ├── task_list_tool.py      # 任务列表工具
  ├── sandbox_web_search_tool.py  # 沙盒搜索工具
  └── computer_use_tool.py   # 电脑操作工具（可选）

docs/
  ├── AGENT_VISUAL_API.md    # API 文档
  └── FRONTEND_INTEGRATION.md # 前端集成指南

tests/
  └── test_agent_visual.py   # 测试用例
```

### 修改文件
```
main.py                       # 添加截图 API、SSE 事件优化
agent/nodes.py               # Agent 循环控制
agent/agent_tools.py         # 工具注册优化
tools/sandbox_browser_tools.py # 添加事件发送
tools/sandbox_browser_session.py # 会话复用优化
```

---

## 🔑 关键参考代码

### Manus 核心文件对应关系

| Manus 文件 | 功能 | Weaver 对应 |
|-----------|------|------------|
| `agentpress/thread_manager.py` | 对话线程管理 | `agent/graph.py` + 改进 |
| `agentpress/response_processor.py` | 响应处理+流式 | `main.py` SSE 部分 |
| `agent/tools/sb_browser_tool.py` | 浏览器工具 | `tools/sandbox_browser_tools.py` |
| `agent/tools/task_list_tool.py` | 任务列表 | 新建 |
| `agent/tools/sandbox_web_search_tool.py` | 网页搜索 | 新建 |
| `sandbox/sandbox.py` | 沙盒管理 | `tools/sandbox_browser_session.py` |
| `agent/run.py` | Agent 运行 | `agent/nodes.py` |

---

## ⏱️ 建议执行顺序

### 第一周：基础功能
- 步骤 1-5（基础架构）
- 步骤 6-7（核心工具）

### 第二周：完善功能
- 步骤 8-12（更多工具 + 控制逻辑）

### 第三周：集成优化
- 步骤 13-17（优化和健壮性）

### 第四周：文档和测试
- 步骤 18-20（文档 + 测试）

---

## ✅ 快速验证（MVP）

如果想快速验证效果，可以先实现最小可行版本：

1. **步骤 3**：截图存储和服务（30分钟）
2. **步骤 5**：SSE 事件格式（1小时）
3. **步骤 7**：浏览器工具返回截图（2小时）
4. **步骤 18**：前端展示示例（1小时）

**预计 4-5 小时可以看到初步效果！**

---

## 🤔 是否需要我开始执行？

请告诉我：
1. 是否同意这个计划？
2. 想从哪一步开始？
3. 是否需要先做 MVP 快速验证？

---

**文档版本**: v1.0.0
**创建日期**: 2025-12-20
**作者**: Weaver Team
