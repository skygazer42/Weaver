# Prompt library plan (step11)
- Keep prompts/templates existing; add version header + usage comments.
- Introduce naming convention: prompts/templates/<domain>/<name>.py with get_prompt() returning str.
- Add optimizer/deepsearch prompts map to config for dynamic selection.
