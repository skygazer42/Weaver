# Full test/static check plan (step22)
- Run pytest -q (done smoke) with pytest-asyncio plugin to avoid skips.
- Add ruff/flake8 config (todo) and run format with black.
- Consider mypy on agent/tools/common modules.
