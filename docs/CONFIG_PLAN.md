# Config split (step17)
- Introduce APP_ENV env var (dev/test/prod) to toggle defaults (logging, tool_approval, human_review).
- Provide .env.dev/.env.prod templates (todo).
- Ensure logs dir exists; allow LOG_LEVEL override.
