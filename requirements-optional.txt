# Optional / heavy dependencies for extra tools.
# Install with:
#   pip install -r requirements-optional.txt
#
# Notes:
# - `browser-use` can cause pip backtracking if left unconstrained; keep it pinned.
# - These are not required to run the unit tests or basic API.

pyautogui==0.9.54
openpyxl==3.1.5
python-pptx==1.0.2
crawl4ai==0.8.0
browser-use==0.11.9
